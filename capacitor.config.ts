import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frog.app',
  appName: 'frog-rater',
  webDir: 'dist/AngularTest/browser',
};

export default config;
